node_modules/
dist/
*.local
.DS_Store

.claude/

# Stray slicer/CLI logs
*.log
/CLAUDE.local.md

# Local agent / design-tooling artifacts — never publish these
/CLAUDE.md
/AGENTS.md
/GEMINI.md
/PRODUCT.md
/DESIGN.md
/DESIGN.sidecar.md
.impeccable/
.cursor/
.windsurf/
.aider*
# Playwright MCP scratch output — captured browser logs, snapshots, screenshots
# of whatever was on screen at the time. `git add -A` would commit the lot.
.playwright-mcp/
